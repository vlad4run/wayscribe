"""Frameless always-on-top recording indicator (PyQt6).

Shows a small floating widget while the daemon is in the RECORDING state:
red dot, elapsed timer, RMS level bar. Phase 4.
"""
from __future__ import annotations


def show() -> None:
    raise NotImplementedError(
        "indicator window is not implemented yet — see README, phase 4."
    )


def hide() -> None:
    raise NotImplementedError(
        "indicator window is not implemented yet — see README, phase 4."
    )
